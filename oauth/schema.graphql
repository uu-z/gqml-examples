# import Mutation.* from "utils/generated/prisma.graphql"
# import Query.* from "utils/generated/prisma.graphql"

directive @private on FIELD_DEFINITION

type User {
  id: ID! 
  createdAt: DateTime!
  updatedAt: DateTime!
  email: String! 
  avatar: String
  githubUserId: String
  password: String @private
  role: Role!
  name: String
}